const fs = require('fs'); const { Document, Packer, Paragraph, TextRun } = require('docx'); const doc = new Document({ sections: [{ children: [ new Paragraph({ children: [ new TextRun({ text: 'CONTRATO DE PRESTAÇÃO DE SERVIÇOS', bold: true, size: 28 }) ] }), new Paragraph({ children: [ new TextRun({ text: 'Contratante: {{customer.name}}', size: 24 }) ] }), new Paragraph({ children: [ new TextRun({ text: 'CNPJ: {{customer.cnpj}}', size: 24 }) ] }), new Paragraph({ children: [ new TextRun({ text: 'O presente contrato tem como objeto o licenciamento do software {{software.name}} pelo valor mensal de {{contract.value}}.', size: 24 }) ] }) ] }] }); Packer.toBuffer(doc).then(buffer => { fs.writeFileSync('modelo_teste.docx', buffer); console.log('Gerado modelo_teste.docx'); });
